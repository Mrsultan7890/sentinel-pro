# ══════════════════════════════════════════════════════════════
#  The Sentinel Pro v3.0 — Requirements
#  Install: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════

# ── Core HTTP & Networking ─────────────────────────────────────
requests[socks]>=2.31.0
httpx[http2,brotli]>=0.25.0
urllib3>=2.0.0
PySocks>=1.7.1

# ── HTML Parsing ───────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=4.9.3

# ── DNS & Network Recon ────────────────────────────────────────
dnspython>=2.4.0
python-whois>=0.9.4

# ── Security & Crypto ─────────────────────────────────────────
cryptography>=41.0.0
pyOpenSSL>=23.0.0

# ── Tor / Proxy ────────────────────────────────────────────────
stem>=1.8.2

# ── Environment & Config ──────────────────────────────────────
python-dotenv>=1.0.0

# ── CLI & UI ──────────────────────────────────────────────────
rich>=13.0.0
pyfiglet>=0.8.0
tqdm>=4.65.0
colorama>=0.4.6

# ── Async ─────────────────────────────────────────────────────
aiohttp>=3.9.0

# ── Rate Limiting ─────────────────────────────────────────────
ratelimit>=2.2.1

# ── OSINT / Intelligence APIs ─────────────────────────────────
shodan>=1.28.0

# ── Data Science & ML Engine (required) ───────────────────────
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
joblib>=1.3.0

# ── NLP Engine ────────────────────────────────────────────────
nltk>=3.8.0
spacy>=3.7.0
langdetect>=1.0.9
# After install: python -m spacy download en_core_web_sm

# ── Network Analysis (relation mapper) ────────────────────────
networkx>=3.1.0

# ── Visualization (reports) ───────────────────────────────────
matplotlib>=3.7.0

# ── Image OSINT ───────────────────────────────────────────────
Pillow>=10.0.0
opencv-python>=4.8.0

# ── Face Analysis (optional — heavy ~500MB) ───────────────────
# deepface>=0.0.79
# tf-keras>=2.15.0      # required by deepface with tensorflow 2.16+

# ── Favicon Hash Fingerprinting ───────────────────────────────
mmh3>=4.0.0

# ── ML Training Data Pipeline ─────────────────────────────────
crawl-relevance-layers>=1.1.1
ddgs>=6.0.0

# ── PDF Export ────────────────────────────────────────────────
weasyprint>=60.0

# ── Fake Profile Detection (optional HuggingFace model) ───────
# transformers>=4.35.0

# ── Custom Neural Network (SentinelThreatNet) ─────────────────
torch>=2.0.0  # CPU version: pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── User Agent Rotation ───────────────────────────────────────
fake-useragent>=1.4.0

# Groq LLM API
groq>=0.9.0

# BPE Tokenizer (Seq2Seq)
tokenizers>=0.19.0

# Browser automation (optional)
playwright>=1.40.0

# System monitoring
psutil>=5.9.0

# Security & Privilege Management
keyring>=24.0.0

# Seaborn (visualization)
seaborn>=0.13.0

# Secure File Manager
cryptography>=41.0.0
python-magic>=0.4.27

# LightGBM (ML engine — log analysis)
lightgbm>=4.0.0

# SentinelProxy UI
# (tkinter is stdlib — no install needed)
# websocket-client for WS replay tab
websocket-client>=1.6.0