# ── Credentials & Secrets ─────────────────────────────────────────────────────
.env
.env.local
.env.*.local
*.key
*.pem
*.p12
*.pfx
*.crt
*.csr
data/.encryption_key
data/file_registry.json
*secret*
*credential*
*api_key*
*token*

# ── IDE / AI assistant internal files ────────────────────────────────────────
.amazonq/
.idea/
.vscode/
.cursor/

# ── User runtime data ─────────────────────────────────────────────────────────
.sentinel_pro/
.sentinel_history
data/sentinel.db
data/sentinel.db-shm
data/sentinel.db-wal
data/sentinel_memory.db
data/sentinel_memory.db-shm
data/sentinel_memory.db-wal
data/sentinel_proxy.db
data/sentinel_proxy.db-shm
data/sentinel_proxy.db-wal
data/*.db
data/*.db-shm
data/*.db-wal

# ── User scan output ──────────────────────────────────────────────────────────
reports/
investigations/
screenshots/
evidence/
temp/
logs/
*.log
backups/

# Keep example target files
targets/
!targets/bugbounty.txt
!targets/monitoring.txt
!targets/osint.txt

# ── ML models (large binary files) ───────────────────────────────────────────
models/ml_engine/*.pt
models/ml_engine/*.pth
models/ml_engine/*.joblib
models/ml_engine/*.pkl
models/ml_engine/*.h5
models/ml_engine/*.onnx
models/ml_engine/training_data/
models/ml_engine/old_models/
models/ml_engine/old_models_backup/
models/ml_engine/checkpoints/
models/ml_engine/rl_qtable.json
models/ml_engine/rl_qtable.backup.json
models/sentineloctopus-0.5b/

# ── Proxy models ──────────────────────────────────────────────────────────────
sentinel_proxy/models/
sentinel_proxy/models/*.pt
sentinel_proxy/models/*.joblib

# ── Sentinel Intel runtime data ───────────────────────────────────────────────
sentinel_intel/data/*.db
sentinel_intel/data/*.db-shm
sentinel_intel/data/*.db-wal
sentinel_intel/data/*.json
!sentinel_intel/data/.gitkeep

# ── Rust build artifacts ──────────────────────────────────────────────────────
analyzer/target/
fuzzer/target/
media_analyzer/target/
sentinel_proxy/rust_core/target/
sentinel_proxy/rust_fuzzer/target/
sentinel_crypto/target/
analyzer/Cargo.lock
fuzzer/Cargo.lock
media_analyzer/Cargo.lock

# ── Go build artifacts ────────────────────────────────────────────────────────
scraper/scraper
dirbuster/dirbuster
network_mapper/network_mapper
predictor/predictor
smuggler/smuggler
stealth_proxy/stealth_proxy
*.exe

# ── Compiled .so files ────────────────────────────────────────────────────────
*.so

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
env/
.venv/
*.egg-info/
dist/
build/
.pytest_cache/

# ── Jupyter / training scripts ────────────────────────────────────────────────
.ipynb_checkpoints/
colab_data/
colab_train.py
data_pipeline.py
data_pipeline_v2.py
data_pipeline_v3.py
rl_train_bg.py
SentinelOctopus_Training.ipynb
SentinelOctopus_IdentityFix.ipynb

# ── Internal planning notes ───────────────────────────────────────────────────
OVERVIEW.md
STARTUP_PLAN.md
osints_plan.md
prox_plan.md
*.plan.md

# ── Proxy runtime data only (payloads are part of the tool) ──────────────────
sentinel_proxy/data/

# ── Tests data ───────────────────────────────────────────────────────────────
tests/data/

# ── Predictive module data ────────────────────────────────────────────────────
modules/predictive/data/
dist_compile/

# ── Tor / proxy sockets ───────────────────────────────────────────────────────
/tmp/sentinel_proxy_v2.sock
/tmp/sentinel_proxy_v2.sock.events

# ── CA certificates (auto-generated) ─────────────────────────────────────────
~/.mitmproxy/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ── Cache ─────────────────────────────────────────────────────────────────────
.crl_cache
*.cache
.cache/
sentinel.db
=*
